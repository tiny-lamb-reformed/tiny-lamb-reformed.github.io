<nav class="nav__list">
  {% if page.sidebar.title %}<h3 class="nav__title" style="padding-left: 0;">{{ page.sidebar.title }}</h3>{% endif %}
  <input id="ac-toc" name="accordion-toc" type="checkbox" />
  <label for="ac-toc">{{ site.data.ui-text[site.locale].menu_label | default: "Toggle Menu" }}</label>
  <ul class="nav__items">
    {% assign ungrouped = site.posts | map: "category" | uniq %}
    {% for group in site.category_groups %}
      <li>
        <span class="nav__sub-title">{{ group.title }}</span>        
        <ul>
          {% for category in group.categories | default: ungrouped %}
            <li><a href="#{{ category | slugify }}">{{ category }}</a></li>
          {% endfor %}
        </ul>
      </li>
    {% endfor %}
  </ul>
</nav>